version: '3.0'

services:
  frontend:
    build: frontend
    ports:
      - 8080:8080
      # entrypoint: [ "/bin/sh", "-c", "sleep 10000"]
    volumes:
      - ./frontend:/root/remote_modular_frontend
  backend:
    build: .
    entrypoint: [ "node", "server.js" ]
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000"]
    ports:
      - 3000:3000
  client:
    build: .
    depends_on:
      - backend
    ports:
      - 57220:57220/udp
    environment:
      - SC_HOST=host.docker.internal
      - SC_PORT=57120
      - BACKEND_ADDRESS=http://backend:3000
